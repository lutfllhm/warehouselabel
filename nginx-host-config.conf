# Konfigurasi Nginx di Host VPS untuk Warehouse Label
# Letakkan file ini di: /etc/nginx/sites-available/warehouselabel
# Lalu buat symlink: sudo ln -s /etc/nginx/sites-available/warehouselabel /etc/nginx/sites-enabled/

# Redirect www to non-www
server {
    listen 80;
    server_name www.warehouselabel.iwareid.com;
    return 301 http://warehouselabel.iwareid.com$request_uri;
}

# Main server block
server {
    listen 80;
    server_name warehouselabel.iwareid.com;

    # Logging
    access_log /var/log/nginx/warehouselabel-access.log;
    error_log /var/log/nginx/warehouselabel-error.log;

    # Proxy to Docker container (port 9000)
    location / {
        proxy_pass http://localhost:9000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
        
        # Timeout settings
        proxy_connect_timeout 60s;
        proxy_send_timeout 60s;
        proxy_read_timeout 60s;
    }

    # Client max body size (untuk upload file)
    client_max_body_size 50M;
}

# HTTPS Configuration (uncomment setelah setup SSL)
# server {
#     listen 443 ssl http2;
#     server_name warehouselabel.iwareid.com;
#
#     ssl_certificate /etc/letsencrypt/live/warehouselabel.iwareid.com/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/warehouselabel.iwareid.com/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#
#     access_log /var/log/nginx/warehouselabel-ssl-access.log;
#     error_log /var/log/nginx/warehouselabel-ssl-error.log;
#
#     location / {
#         proxy_pass http://localhost:9000;
#         proxy_http_version 1.1;
#         proxy_set_header Upgrade $http_upgrade;
#         proxy_set_header Connection 'upgrade';
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_cache_bypass $http_upgrade;
#     }
#
#     client_max_body_size 50M;
# }
#
# # Redirect HTTP to HTTPS
# server {
#     listen 80;
#     server_name warehouselabel.iwareid.com www.warehouselabel.iwareid.com;
#     return 301 https://warehouselabel.iwareid.com$request_uri;
# }
